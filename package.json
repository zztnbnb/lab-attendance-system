{
  "name": "labtime-desktop",
  "version": "0.1.7",
  "private": true,
  "main": "electron/main.cjs",
  "scripts": {
    "electron:dev": "npm --prefix frontend run build && electron electron/main.cjs --dev",
    "electron:build": "powershell -ExecutionPolicy Bypass -File scripts/build-installer.ps1"
  },
  "devDependencies": {
    "electron": "^36.4.0",
    "electron-builder": "^26.0.12"
  }
}
